{% macro configWarning(setting) -%}
    {{ 'This is being overridden by the `{setting}` config setting in `config/insights.php`.'|t('insights', { setting: setting })|markdown(inlineOnly=true) }}
{%- endmacro %}
